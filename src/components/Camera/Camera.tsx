/**
 * Camera.tsx
 *
 * Thin re-export of the canonical CameraFeed component.
 * All camera + pose logic lives in CameraFeed.tsx.
 */
export { default as CameraFeed } from './CameraFeed';
export { default } from './CameraFeed';
